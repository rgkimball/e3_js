<?php

/**
 * Implements hook_init()
 */
function e3_js_init() {
  $path = explode('/', current_path());

  if ($path[0] != 'admin') {
    $js_path = drupal_get_path('module', 'e3_js') . '/e3_js.js';
    drupal_add_js($js_path, array('scope' => 'header', 'preprocess' => true), 'setting');
  }

}