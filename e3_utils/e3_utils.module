<?php

/**
 * Implements hook_init()
 */
function e3_utils_init() {

	if(!path_is_admin(current_path())) {
		$js_path = drupal_get_path('module', 'e3_utils') . '/e3_utils.min.js';
		drupal_add_js($js_path, array('scope' => 'header', 'preprocess' => true), 'setting');
	}

}